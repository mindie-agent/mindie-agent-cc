# P0/P1 follow-up evidence

The configured local provider remains in use: native Claude Code 2.1.269
reports deepseek-flash/high. The user explicitly excluded OAuth-only testing;
it is not an outstanding acceptance requirement.

A fresh native profile and project completed pinned setup, actual plugin
installation, first-use choices and persisted read-only selection. In the
interactive native default permission mode, query and explain each received
one ordinary user approval and succeeded (two results, 2502 body characters).
Contribution stayed disabled: zero captures, organizer attempts and outbox
entries. The idle read service and temporary provider configuration were
cleaned up. An earlier headless default-permission denial is retained as a
denial, not counted as a successful read.

The revised organizer faithfully retained recorded NPU values in 13.27 seconds;
real activation/configuration-only material returned no entries in 1.57 seconds.
Missing details are omitted, without inferred lessons or an unknown checklist.

Actual GitHub feed success plus a missing repository now reports `degraded`;
real lock contention reports `deferred`, and recovery clears the error while
retaining content. Scheduler registration selects the current generation's
launcher. Its actual macOS first dispatch under an isolated 30-second test
label completed in 32.66 seconds, runs=1/exit=0, using installed main d8545a3.
All timer labels were removed. Timer delivery and new feed-result checks are
separate evidence; no model call or automatic retry was used for either.

Final contribution/Bot/new-task integration and Windows real-host acceptance
are separate from the above results. No Anthropic-model acceptance is claimed.
